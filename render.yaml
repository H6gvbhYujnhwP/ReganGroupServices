services:
  - type: web
    name: ReganGroupServices
    env: static
    buildCommand: npm install -g pnpm@10 && cd client && pnpm install && pnpm run build
    staticPublishPath: client/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
