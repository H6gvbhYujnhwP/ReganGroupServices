services:
  - type: web
    name: ReganGroupServices
    env: static
    buildCommand: npm install -g pnpm@10 && pnpm install && pnpm run build
    staticPublishPath: dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
